import React from 'react';
import logo from '../../logo.png';

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="logo" className="logo" />
        </div>
    );
};

export default Logo;
